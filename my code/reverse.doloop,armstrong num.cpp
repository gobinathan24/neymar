/*gobi
while*/

#include<stdio.h>
int main()
{ 
int num,rem,rev=0,temp;
printf("enter the num:");
scanf("%d",&num);
temp=num;
    while(num!=0)
	{ 
 	rem= num %10;
 	rev= rev+("%d/1||%d");
 	num= num/10;
    }
    printf("%d\n",rev);
	if
	 printf("%d is armstrong",rev);
	else
	  printf("%d is not armstrong",rev);
}
